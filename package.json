{
  "name": "recipe-friend",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "recipe-friend-mcp": "./mcp/index.js"
  },
  "description": "A recipe box that syncs across the devices you cook from, and an MCP server that lets a household's assistant read the book and help with the week's plan (J16). The site is plain HTML/CSS/JS with no build step; the dependencies below are the server's alone.",
  "files": [
    "js",
    "mcp",
    "test/helpers"
  ],
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/malfernion/Recipe-friend.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@supabase/supabase-js": "^2.116.0"
  }
}
